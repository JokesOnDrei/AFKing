# AFKing tray app (Windows).
# The keep-awake engine itself uses only the Python standard library (ctypes);
# these two are just for the system-tray icon UI.
pystray>=0.19
Pillow>=10.0

# The legacy cross-platform console script (away.py) instead needs:
#   pip install pyautogui pynput
