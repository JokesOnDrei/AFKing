# UTF-8
#
# PyInstaller version-info resource. Embedded into AFKing.exe so the file
# has proper metadata (Properties -> Details) instead of blank fields.
# A filled-in version block looks intentional rather than throwaway/malware.
#
# NOTE: bump the numbers below when you cut a new build. `filevers` and
# `prodvers` are 4-tuples of integers (major, minor, patch, build).
VSVersionInfo(
  ffi=FixedFileInfo(
    filevers=(1, 0, 0, 0),
    prodvers=(1, 0, 0, 0),
    mask=0x3f,
    flags=0x0,
    OS=0x40004,
    fileType=0x1,
    subtype=0x0,
    date=(0, 0),
  ),
  kids=[
    StringFileInfo(
      [
        StringTable(
          '040904B0',
          [
            StringStruct('CompanyName', 'AFKing'),
            StringStruct('FileDescription', 'AFKing - keeps your session active'),
            StringStruct('FileVersion', '1.0.0.0'),
            StringStruct('InternalName', 'AFKing'),
            StringStruct('LegalCopyright', 'Copyright (C) 2026 AFKing'),
            StringStruct('OriginalFilename', 'AFKing.exe'),
            StringStruct('ProductName', 'AFKing'),
            StringStruct('ProductVersion', '1.0.0.0'),
          ],
        )
      ]
    ),
    VarFileInfo([VarStruct('Translation', [1033, 1200])]),
  ],
)
